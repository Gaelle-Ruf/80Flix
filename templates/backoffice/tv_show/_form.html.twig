    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        <button class="badge btn-bo edit">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
